export enum Roles {
  Admin = 1,
  Client = 2,
  Staff = 3,
  User = 4,
  None = 5
}

export enum EmployeeRoles {
  Manager = 1,
  Staff = 2
}

export enum EventStatuses {
  Active = 1,
  Hidden = 2
}

export enum EventTypes {
  Default = 1
}
